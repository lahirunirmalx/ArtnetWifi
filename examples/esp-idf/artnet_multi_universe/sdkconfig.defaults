CONFIG_ESP_MAIN_TASK_STACK_SIZE=4096

# One packet per universe arrives as a burst per frame. The default mailbox of
# 6 drops anything past the sixth packet before this code ever sees it.
CONFIG_LWIP_UDP_RECVMBOX_SIZE=32
